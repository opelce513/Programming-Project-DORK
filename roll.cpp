#include <iostream>
#include <ctime>
#include <cstdlib>
using namepspace std;

rollDice();
{
printw("Press 'r' to roll again \n")
    if(char == 'r')
    {
      int dice;
      srand(time(NULL));
      dice = rand() % 6 + 1;
      return dice;
    }
    else()
    {
      printw("Please press 'r' to roll again \n);
    }
}

























      
